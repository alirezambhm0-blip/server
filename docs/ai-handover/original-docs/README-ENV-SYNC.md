# همگام‌سازی محیط (Env Sync) — رفع 2537 خطای ESLint

## علت مشکل (خلاصه)

تشخیص با داده‌های `diag.txt` قطعی شد:

- `package.json` فعلی تو با نسخه تأییدشده پروژه **متفاوت** است (هش‌ها یکی نیست).
- به همین دلیل `npm ci` تو **۸۴۴ پکیج** نصب می‌کند، درحالی‌که درخت سالم و تأییدشده **۸۱۳ پکیج** است.
- آن ۳۱ پکیج اختلاف باعث می‌شود ESLint نتواند تایپ‌های ماژول‌ها را resolve کند و سونامی
  `Unsafe ... of a type that could not be resolved` / `error typed` (به شکل ثابتِ 2537 خطا) ایجاد شود.
- کد، `eslint.config.mjs` و `tsconfig.json` تو کاملاً سالم و با نسخه من یکسان است — **هیچ مشکلی در کد Phase 5-1 نیست**.

## محتویات این بسته

```
wholesale-api/package.json      (نسخه تأییدشده — SHA256: 7033bdbfb6e8b880d86c0a77120c407a58733d08e7b1c6c79f439c8b35db7e18)
wholesale-api/package-lock.json (لاک‌فایل تأییدشده متناظر)
```

## مراحل اعمال (در CMD)

1) زیپ را در روت پروژه استخراج کن (روی فایل‌های موجود overwrite شود):

```
C:\Users\sam\Desktop\workspace-01
```

2) نصب تمیز دقیقاً از روی لاک‌فایل جدید:

```cmd
cd C:\Users\sam\Desktop\workspace-01\wholesale-api
rmdir /s /q node_modules
npm ci
npx prisma generate
```

3) اعتبارسنجی:

```cmd
npx eslint src && npx tsc --noEmit && npm run build
```

## نتیجه مورد انتظار (تأییدشده روی محیط تمیز، ۴ بار پیاپی)

- `npm ci` → پیام **added 813 packages** (نه 844!)
- `npx eslint src` → بدون خروجی (۰ مشکل)
- `npx tsc --noEmit` → بدون خروجی (۰ خطا)
- `npm run build` → موفق
- `npx eslint -v` → نسخه v9.39.4

## اگر بعد از این هم خطا ماند

تنها متغیر باقی‌مانده نسخه Node است: روی سیستم تو **v24.16.0** است، درحالی‌که تأیید ما با **v20 LTS** انجام شده.
در آن صورت Node 20 LTS (یا 22 LTS) را نصب و همین مراحل ۲ و ۳ را تکرار کن و نتیجه را اطلاع بده.

## نکته برای آینده

- از این به بعد فقط با `npm ci` (یا `npm install` خالص) نصب کن؛ `npm update` یا نصب دستی نسخه‌های جدیدتر نزن تا درخت با لاک‌فایل یکی بماند.
- توجه: باید مطمئن شویم پوشه کاری تو همیشه از آخرین سری فایل‌های تحویلی ساخته می‌شود؛ `package.json` فعلی‌ات از نسخه‌ای قدیمی‌تر بوده.
